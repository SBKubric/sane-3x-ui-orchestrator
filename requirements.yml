---
# Collections used by the roles (ansible-galaxy collection install -r requirements.yml).
collections:
  # Self-signed panel certificate (role panel, #2).
  - name: community.crypto
    version: "3.4.0"
  - name: community.general
    version: "13.4.0"
