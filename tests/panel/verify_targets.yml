---
# The monitoring targets step of verify.yml alone, against mock_panel.py (see test_panel_inbounds.py).
- name: Verify monitoring targets
  hosts: panel
  gather_facts: false
  tasks:
    - name: Verify that every target is UP
      ansible.builtin.include_role:
        name: panel
        tasks_from: verify_targets
